<!DOCTYPE html>
<html>
<head>
	<title>Working with mySQL</title>
</head>
<body>
	<?php 
		// a database is a set of tables that stores ralated data
		// i database  = 1 application
	// Access permissions are granted at database level
	// Table -set of columns and rows
	// A table represents a single concept (a noun
	// create a relationship between tables
	// a column is a set of data of a single simple type
	// a row is a single record of data
	// a field is an intersection of a column and a row
	// an index is a data structure on a table used to increase look up speed
	// foreign key is a table  column whose values reference rows in a nother table 
	// the foundation
	 ?>
</body>
</html>